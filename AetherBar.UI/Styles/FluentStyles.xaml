<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <SolidColorBrush x:Key="FluentAccent" Color="#FF4444"/>
    <SolidColorBrush x:Key="FluentAccentDark" Color="#CC0000"/>
    <SolidColorBrush x:Key="SurfaceColor" Color="#2D2D2D"/>
    <SolidColorBrush x:Key="SurfaceAltColor" Color="#3D3D3D"/>
    <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="TextSecondary" Color="#999999"/>
    <SolidColorBrush x:Key="TextTertiary" Color="#606060"/>
    <SolidColorBrush x:Key="CardBackground" Color="#252525"/>
    <SolidColorBrush x:Key="CardBorder" Color="#333333"/>
    <SolidColorBrush x:Key="CardHover" Color="#2A2A2A"/>
    <SolidColorBrush x:Key="TabInactive" Color="#2D2D2D"/>
    <SolidColorBrush x:Key="TabHover" Color="#353535"/>
    <SolidColorBrush x:Key="TitleBarBg" Color="#1A1A1A"/>
    <SolidColorBrush x:Key="FooterBg" Color="#1A1A1A"/>
    <SolidColorBrush x:Key="WindowBackground" Color="#1E1E1E"/>
    <SolidColorBrush x:Key="CustomColorPanelBg" Color="#1E1E1E"/>
    <SolidColorBrush x:Key="PopupBackground" Color="#1E1E1E"/>
    <SolidColorBrush x:Key="PopupBorder" Color="#3D3D3D"/>
    <SolidColorBrush x:Key="ComboBoxBackground" Color="#3D3D3D"/>
    <SolidColorBrush x:Key="ComboBoxForeground" Color="White"/>
    <SolidColorBrush x:Key="ComboBoxHover" Color="#4D4D4D"/>
    <SolidColorBrush x:Key="ComboBoxSelected" Color="#505050"/>
    <SolidColorBrush x:Key="ButtonHover" Color="#5D5D5D"/>
    <SolidColorBrush x:Key="ButtonPressed" Color="#6D6D6D"/>
    <SolidColorBrush x:Key="TabHeaderBg" Color="#151515"/>
    <SolidColorBrush x:Key="TabHeaderBorder" Color="#2A2A2A"/>
    <SolidColorBrush x:Key="SliderArrow" Color="#999999"/>
    <SolidColorBrush x:Key="SliderTrack" Color="#3D3D3D"/>
    <SolidColorBrush x:Key="SliderTrackHover" Color="#4D4D4D"/>
    <SolidColorBrush x:Key="SliderThumb" Color="#CCCCCC"/>
    <SolidColorBrush x:Key="SliderThumbHover" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="SliderTickBar" Color="#555555"/>
    <SolidColorBrush x:Key="SliderBorder" Color="#333333"/>

    <!-- Card container -->
    <Style x:Key="FluentCard" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource CardBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Margin" Value="0,0,0,16"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="16" ShadowDepth="3" Opacity="0.35" Color="Black"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TabControl: horizontal tabs with underline indicator -->
    <Style x:Key="FluentTabControl" TargetType="TabControl">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="TabStripPlacement" Value="Top"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="{DynamicResource TabHeaderBg}" Padding="16,0"
                                BorderThickness="0,0,0,1" BorderBrush="{DynamicResource TabHeaderBorder}">
                            <TabPanel x:Name="HeaderPanel"
                                      IsItemsHost="True"
                                      Panel.ZIndex="1"/>
                        </Border>
                        <Border Grid.Row="1"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="24,16,24,0">
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                              ContentSource="SelectedContent"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                    <Setter Property="FontSize" Value="13"/>
                    <Setter Property="Padding" Value="16,10"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Border"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="4,4,0,0"
                                        Padding="{TemplateBinding Padding}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock x:Name="IconContent"
                                                   Text="{TemplateBinding Tag}"
                                                   FontFamily="Segoe MDL2 Assets"
                                                   FontSize="12"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,6,0"/>
                                        <TextBlock x:Name="TabText"
                                                   Text="{TemplateBinding Header}"
                                                   FontSize="13"
                                                   FontWeight="Normal"
                                                   Foreground="{TemplateBinding Foreground}"/>
                                    </StackPanel>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="Transparent"/>
                                        <Setter Property="Foreground" Value="{DynamicResource FluentAccent}"/>
                                        <Setter TargetName="TabText" Property="FontWeight" Value="SemiBold"/>
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsMouseOver" Value="True"/>
                                            <Condition Property="IsSelected" Value="False"/>
                                        </MultiTrigger.Conditions>
                                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource TabHover}"/>
                                    </MultiTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Section Header -->
    <Style x:Key="SectionHeader" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>

    <!-- Card Title (first item in a card) -->
    <Style x:Key="CardTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="Margin" Value="0,0,0,12"/>
    </Style>

    <!-- Value label next to sliders -->
    <Style x:Key="SliderValue" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Width" Value="32"/>
        <Setter Property="TextAlignment" Value="Right"/>
    </Style>

</ResourceDictionary>
